// This file was generated by [ts-rs](https://github.com/Aleph-Alpha/ts-rs). Do not edit this file manually.
import type { BlendMode } from "./BlendMode";
import type { Decal } from "./Decal";
import type { Font } from "./Font";
import type { LetterAnimation } from "./LetterAnimation";
import type { LetterOverride } from "./LetterOverride";
import type { Rgba } from "./Rgba";
import type { SurfaceShape } from "./SurfaceShape";
import type { Track } from "./Track";

/**
 * What a layer actually draws. Internally tagged so the TS side is a clean
 * discriminated union on `kind`.
 */
export type LayerKind = { "kind": "image", src: string, width: number, height: number, attach: Decal | null, } | { "kind": "shape3d", shape: SurfaceShape, width: number, height: number, 
/**
 * Box depth (px). Ignored for cylinders.
 */
depth: number, rotation_x: Track, rotation_y: Track, rotation_z: Track, 
/**
 * 0 = orthographic, 1 = full perspective foreshortening.
 */
perspective: number, 
/**
 * Camera distance (px-ish). Larger = flatter perspective.
 */
focal_length: number, 
/**
 * Cylinder: degrees of circumference shown (0..360). Ignored for boxes.
 */
coverage: number, 
/**
 * Cylinder: radius (px). Ignored for boxes.
 */
radius: number, } | { "kind": "text", content: string, 
/**
 * Font size in px (the letter "height").
 */
size: number, color: Rgba, font: Font, anim: LetterAnimation | null, parts: Array<LetterOverride>, 
/**
 * Keyframeable 0..1: how much of `parts` is applied. 0 = composed,
 * 1 = fully decomposed. Keyframe it to animate the decompose effect.
 */
decompose: Track, } | { "kind": "colorpatch", color: Rgba, blend: BlendMode, width: number, height: number, };
